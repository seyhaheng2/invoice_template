.layout-content-body
  .row.gutter-xs
    .col-xs-12
      .card
        .card-header
          .card-actions
            = link_to new_user_path, class: 'card-action' do
              %span.glyphicon.glyphicon-plus
            %button.card-action.card-toggler{:title => "Collapse", :type => "button"}
            %button.card-action.card-reload{:title => "Reload", :type => "button"}
            %button.card-action.card-remove{:title => "Remove", :type => "button"}
          %strong តារាងឈ្មោះអតិថិជន
        .card-body
          %table#demo-datatables-1.table.table-striped.table-nowrap.dataTable{:cellspacing => "0", :width => "100%"}
            %thead
              %tr
                %th លេខ
                %th Profile
                %th ឈ្មោះ
                %th ភេទ
                %th លេខទូរស័ព្ទ
                %th អាស័យដ្ឋាន
                %th ពត៌មានអំពីគាត់
            %tbody
              - @users.each do |user|
                %tr
                  %td.col-xs-1= user.id
                  %td.col-xs-1= user.profile
                  %td.col-xs-= user.name
                  %td.col-xs-= user.sex
                  %td.col-xs-= user.phone
                  %td.col-xs-= user.address
                  %td.col-xs-= user.description
